# Slice 38G — Predicate, Role, and Frame Candidate Proposal

This slice connects accepted Slice 37F candidate output to accepted Slice 38 registries without selecting meaning or creating operational authority.

## Source package

`aiweb_language_core_bootstrap/predicate_role_frame_registry/predicate_role_frame_candidate_proposal`

## Public operation

`propose_predicate_role_frame_candidates(...)`

## Canonical behavior

The canonical compatibility snapshot has zero rules and zero conflicts because no current Slice 37 concept or sense is an exact action-root identity. Candidate-bearing Slice 37 results therefore return explicit unsupported state. Unknown and unsupported predecessor states remain explicit.

## Controlled future behavior

A caller may supply an exact immutable compatibility snapshot. The operation can then preserve zero, one, or many action-root/predicate, frame/role-layout, and capability-reference candidates. Every result remains unselected and non-operative.

## Verification

Run:

`python3 -B scripts/test_aiweb_slice38g_predicate_role_frame_candidate_proposal.py /home/nic/forge`

Then run the visible independent verifier in the appropriate mode.
