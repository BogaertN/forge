# AI.Web Slice 42G Operator README

Slice 42G is an additive, offline, deterministic MSM-v1 custody adapter.

## What it does

It consumes the exact accepted Slice 41E selected-meaning integration and exact accepted Slice 42F unvalidated expression candidate. It creates one immutable MSM-v1 successor containing exactly one governed outward meaning and exactly one expression link, plus the authority reference and lifecycle traces required by the existing MSM lifecycle law.

## What it does not do

It does not rewrite MSM-v1, validate through Echo, authorize delivery, deliver text, grant truth or evidence status, perform actions, access memory, load resources, use models or similarity systems, enable the bootstrap, close Slice 42, or supersede GP-014.

## Required operator sequence

1. Verify the external package and checksum.
2. Inspect archive safety.
3. Extract once.
4. Run the read-only package inspector.
5. Confirm the exact accepted Slice 42F base and clean repository.
6. Apply exactly the 15 additive paths.
7. Run the visible Slice 42G behavior test and inherited verifier through the result collector.
8. Upload the result archive and checksum for independent review.
9. Do not stage or commit until the applied evidence is accepted.

The package includes an exact rollback utility for application failure. Do not run rollback after a successful applied proof unless a later evidence review explicitly requires it.
