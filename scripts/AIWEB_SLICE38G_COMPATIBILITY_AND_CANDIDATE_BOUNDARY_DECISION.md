# Slice 38G Compatibility and Candidate Boundary Decision

## Decision

Admit the candidate-proposal runtime and an exact compatibility-snapshot schema. Do not admit any canonical Slice 37-to-Slice 38 action-root mapping in this slice.

## Reason

The accepted Slice 37 registry currently contains controlled identities for governed semantic resources, source expression forms, concept admission, and unknown concept conditions. Treating any of those identities as equivalent to `inspect`, `report`, `request`, `verify`, or `simulate` would be an unsupported semantic leap. The lawful production decision is a closed empty canonical bridge and explicit unsupported output.

## Future exact admissions

A later compatibility admission may identify an exact concept and/or sense, an exact action root, its exact predicate, allowed exact frames, scope, provenance, lifecycle state, and visible conflicts. It may not perform surface matching, normalization, nearest-known substitution, semantic similarity, model inference, ranking, selection, permission, routing, or execution.

## Candidate states

- `candidates_proposed`
- `structurally_incomplete`
- `ambiguous`
- `conflicted`
- `explicit_unknown`
- `explicit_unsupported`
- `predecessor_rejected`

## Permanent distinctions

- concept candidate != action root
- sense candidate != predicate identity
- exact compatibility != selected predicate
- frame candidate != selected frame
- role-layout candidate != participant assignment
- missing role != clarification outcome
- conflict != refusal outcome
- structurally complete != selected meaning
- capability reference != availability
- availability != route
- route != invocation
- invocation proposal != execution
- execution != proof
