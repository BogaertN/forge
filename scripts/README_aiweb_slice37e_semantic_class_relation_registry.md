# Slice 37E — Semantic Classes and Relation-Type Rules

This additive package installs the minimal controlled semantic organization layer after the accepted Slice 37D registry.

## Closed set

- 6 semantic classes;
- 6 explicit concept memberships;
- 5 relation families;
- 6 relation types;
- 1 explicit component/composition inverse declaration;
- 6 current relation-version identities;
- 4 unresolved relation-state policies;
- 16 prohibited implication rules;
- 0 relation instances.

## Safety boundary

Class membership does not create authority. Relation types do not create facts. Relation eligibility does not create an instance. Relation instances are not truth. Source, evidence, verification, status, and architecture relations cannot establish reliability, validity, verified status, status application, or implementation.

No accepted predecessor is modified. No route, runtime, tool, memory, action, renderer, delivery path, external resource, LLM, embedding, vector, similarity process, or automatic graph inference is installed.

## Verification

Run:

```bash
PYTHONDONTWRITEBYTECODE=1 /usr/bin/python3 -B scripts/test_aiweb_slice37e_semantic_class_relation_registry.py
PYTHONDONTWRITEBYTECODE=1 /usr/bin/python3 -B scripts/aiweb_slice37e_semantic_class_relation_registry_verify.py /home/nic/forge --mode precommit
```

The committed verifier mode is reserved for the exact separately authorized Slice 37E commit.

## Component/composition family separation

Document 4 Section 30.18 and Section 30.19 are represented by distinct relation-family identities. The inverse-facing relation types remain separately versioned and create no graph edge.
