# Bridge 5A Runtime Specification

Bridge 5A is a compatibility correction at the Slice 39G to Slice 41C validation seam.

The accepted runtime relation is:

- the MSM manifest record owns an MSM lineage;
- the exact Slice 39G companion preserves the source CandidateMeaning lineage;
- the two records are linked through exact typed references, not identifier equality between those separate lineage domains.

The validator must continue to require every existing exact record, companion, candidate custody, gate custody, composition, family result, disposition, limitation, and governance reference.

The correction changes no constructor, no candidate custody adapter, no gate runtime, no evaluator outcome logic, no selected-meaning constructor, no Forge command route, and no operator console route.

Acceptance requires the real Slice 37→38→39F→39G chain, Slice 40H custody, and Slice 41C input validation to succeed while the two lineage domains remain distinct.
