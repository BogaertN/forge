# AI.Web Slice 42A Outward Expression Runtime Core Schema Specification

## Status

Schema-only increment.  No validation engine, identity calculator, authority
admission, eligibility evaluator, obligation projector, outward-meaning
constructor, planner, realizer, MSM adapter, Echo validator, or bootstrap
integration is installed.

## Accepted parent

- Repository: `/home/nic/forge`
- Branch: `main`
- HEAD: `661ff1e17d8d4a982641ca39dc150b23bbb766e9`
- Tree: `e56c9af88be9b845de534c62c9b82fa6af960f3f`
- Subject:
  `Slice 41F disabled bootstrap integration and Slice 41 closeout`

## Package

`aiweb_language_core_bootstrap.outward_expression_runtime`

## Purpose

Create immutable typed companion records that can later preserve the exact
authorized selected-meaning chain, outward-expression authority requirements,
and all outbound preservation obligations without authorizing expression or
producing text.

## Admitted schema types

1. `SelectedMeaningExpressionSourceCustodyRecord`
2. `OutwardExpressionAuthorityRequirementRecord`
3. `ExpressionPreservationObligationCustodyRecord`
4. `ExpressionEligibilityStatusRecord`
5. `GovernedOutwardMeaningBoundaryRecord`
6. `ExpressionPlanBoundaryRecord`
7. `RealizedExpressionBoundaryRecord`
8. `ExpressionTraceBoundaryRecord`
9. `ExpressionReceiptBoundaryRecord`
10. `OutwardExpressionRuntimeSchemaRecord`

Schema-only enums:

1. `ExpressionEligibilityCustodyState`
2. `OutwardMeaningCustodyState`
3. `ExpressionPlanCustodyState`
4. `RealizedExpressionCustodyState`

## Exact predecessor custody

The schema can preserve exact references to:

- Slice 41E integration input, result, receipt, source manifest, and immutable
  successor manifest;
- the integrated `SelectedGovernedMeaningRecord`;
- the exact selected candidate;
- the selection authority reference;
- the Slice 41C eligibility result;
- the Slice 41D decision, content proof, trace, and receipt;
- the Slice 41F acceptance record;
- every preserved alternative;
- unresolved alternatives;
- ambiguity and clarification ancestry;
- inherited limitations;
- blocked consequences;
- refusal-relevant conditions;
- authority-sensitive distinctions;
- preservation classes.

## Outbound preservation custody

The schema separately represents:

- active scope;
- certainty;
- evidence status;
- inherited limitations;
- required caveats;
- refusal boundaries;
- unresolved conditions;
- memory authority;
- external-resource status;
- delivery authority;
- ambiguity;
- privacy and identity boundaries.

## Exact boundary chain

`SelectedMeaningExpressionSourceCustodyRecord`
→ `OutwardExpressionAuthorityRequirementRecord`
→ `ExpressionPreservationObligationCustodyRecord`
→ `ExpressionEligibilityStatusRecord`
→ `GovernedOutwardMeaningBoundaryRecord`
→ `ExpressionPlanBoundaryRecord`
→ `RealizedExpressionBoundaryRecord`
→ trace and receipt boundaries.

This chain is schema custody only.  No transition in that chain has occurred in
Slice 42A.

## MSM-v1 decision

Decision value:

`deferred_to_slice42g_exact_additive_adapter`

The accepted MSM-v1 schema already contains dormant governed-outward-meaning and
expression-link records.  Slice 42A does not instantiate them, modify MSM-v1,
or perform automatic migration.

## Predecessor expression scaffold

`aiweb_output_expression_boundary_scaffold` remains protected Slice 17 evidence.
It is not imported, wrapped, replaced, or promoted by Slice 42A.

## Hard boundary

Slice 42A does not:

- calculate identities;
- validate, serialize, or deserialize records;
- perform lifecycle transitions;
- admit an authority record;
- infer authority from selected meaning;
- evaluate expression eligibility;
- project obligations;
- construct governed outward meaning;
- construct an expression plan;
- realize or hash human-readable text as an output;
- create a realization trace or receipt;
- perform Echo validation;
- create MSM outward or expression-link records;
- modify or migrate MSM-v1;
- enable bootstrap integration;
- create truth, evidence, permission, execution, route, API, tool, action,
  memory, filesystem, network, external-resource, delivery, model, embedding,
  vector, RAG, similarity, neural-parser, hidden-classifier, release, or
  production authority;
- supersede GP-014.

## Deferred work

All Slice 42B through 42H work and Slice 43 remain separately authorized,
implemented, tested, verified, and accepted.
