# AI.Web Slice 38A Schema-Only and Deferred-Scope Decision

Slice 38A is limited to the core record architecture for action roots and
predicate identities. Zero registry entries are admitted.

The following remain deferred:

1. lifecycle transition law and collection validation — Slice 38B;
2. admitted built-in action roots — Slice 38C;
3. participant-role identities — Slice 38D;
4. predicate frames and role compatibility — Slice 38E;
5. capability-family references and effect boundaries — Slice 38F;
6. occurrence-level predicate, role and frame candidates — Slice 38G;
7. disabled integration and closeout — Slice 38H;
8. CandidateMeaning construction — Slice 39;
9. gate selection and ambiguity disposition — Slice 40.

No word token, surface verb, concept, semantic relation, external resource,
capability, route, tool, UI label, prior example, dictionary entry, vector,
embedding, semantic-similarity score, corpus frequency, or LLM explanation may
create a predicate identity.

The accepted Slice 37 runtime remains unchanged and is not superseded.

Schema structure is not action authority. Registry scale is not authority, and scale is not capability.
