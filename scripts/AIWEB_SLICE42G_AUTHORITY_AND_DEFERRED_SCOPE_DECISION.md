# AI.Web Slice 42G Authority and Deferred-Scope Decision

## Controlling purpose

Slice 42G integrates the exact accepted Slice 41E selected-meaning MSM successor and the exact accepted Slice 42F unvalidated expression candidate into one new immutable MSM-v1 successor.

## Source-grounded design ruling

The accepted source already contains dormant `GovernedOutwardMeaningRecord`, `ExpressionLinkRecord`, `ExternalAuthorityReferenceRecord`, `SemanticTransitionTraceRecord`, and `MeaningStructureManifestV1` sections for governed outward meaning and expression-link custody. The accepted Slice 42A authority record explicitly defers outward MSM integration to a Slice 42G exact additive adapter.

Therefore Slice 42G:

1. does not rewrite or migrate MSM-v1;
2. uses the existing dormant record types unchanged;
3. appends exactly one external authority reference for the exact Slice 42F candidate;
4. appends exactly one governed outward meaning based on the exact selected meaning and candidate-preservation obligations;
5. appends exactly one expression link joining that outward meaning to the exact unvalidated candidate;
6. appends exactly two authorized ancestry transition traces;
7. creates a deterministic versioned custody companion and receipt outside MSM-v1;
8. validates the complete successor manifest;
9. preserves every existing candidate, non-selection outcome, selected meaning, governed-result reference, validation link, delivery link, authority reference, and transition trace.

## Hard boundary

An expression link is custody, not Echo validation, approval, delivery authorization, delivery, truth, evidence validation, permission, execution, route, tool, action, memory, filesystem, network, external-resource loading, model authority, or GP-014 supersession.

The integrated expression candidate remains unvalidated. Echo validation remains deferred to Slice 43. Slice 42 closeout and bootstrap enablement remain deferred.
