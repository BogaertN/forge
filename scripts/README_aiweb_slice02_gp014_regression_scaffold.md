# AI.Web Slice 2 — GP-014 Regression Scaffold

This directory and script set belong to Slice 2 only.

Slice 2 preserves GP-014 / LANG-EXPR-001 as the accepted bounded mathematical-output expression baseline while creating a deterministic regression receipt scaffold around it.

This scaffold does not modify GP-014.
It does not replace GP-014.
It does not supersede GP-014.
It does not repair GP-015.
It does not install GP-015R1.
It does not create general language authority.
It does not create memory, evidence, corpus, external-resource, UI, delivery, or action-routing authority.

Run verifier:

```bash
python3 scripts/aiweb_slice02_gp014_regression_verify.py /home/nic/forge
```

Run behavior test:

```bash
python3 scripts/test_aiweb_slice02_gp014_regression_scaffold.py /home/nic/forge
```

Acceptance remains scoped to the proof scaffold only.
