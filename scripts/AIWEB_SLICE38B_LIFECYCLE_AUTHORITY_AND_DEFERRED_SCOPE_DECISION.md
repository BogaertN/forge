# AI.Web Slice 38B Authority and Deferred-Scope Decision

## Governing ruling

The controlling build authority is **AI.Web Forge Canonical Production Roadmap
v1.0 / 7-12-2026**. The permanent architecture basis is Document 5, especially
its unknown-state, provenance, versioning, lifecycle, no-silent-mutation,
no-silent-promotion, and safe-failure law. Document 9 supplies the proof and
acceptance boundary.

The exact accepted live parent is:

```text
HEAD:    2809966f62d172cf8660f9acb343a92813e87d2b
Parent:  f891a33487ea8bc811243627f1d834be7a43f972
Tree:    b02d41d21c72e7eae3c39ce04e71286b1b5bcbb0
Subject: Slice 38A action-root and predicate-identity core schema
```

## Decision

Slice 38B shall add one isolated `governed_lifecycle` subpackage and its
behavior test, independent verifier, predecessor checksum manifest, runtime
specification, and operator README. It shall not modify any accepted Slice 38A
source file.

The subpackage shall provide explicit scope/non-scope separation and:

- stable lineage identity separate from immutable version-record identity;
- strict canonical version parsing and same-major compatibility;
- fail-closed resource and provenance validation;
- explicit human-approved lifecycle-authority records;
- an explicit lifecycle transition matrix;
- pure lifecycle-transition evaluation and fail-closed assertion;
- collection-level duplicate, reference, ancestry, admission-history, and
  current-state checks;
- exception-safe malformed-record refusal;
- exact-boolean review and authority gates;
- current-terminal dependency custody for active resources;
- permitted-use widening refusal inside one lineage;
- exact rejection-lineage, distinct reopening-authority, quarantine-cause,
  quarantine-release-requirement, withdrawal, and supersession custody checks;
- permanent refusal of nearest-known and similarity-based substitution.

The accepted Slice 38A state `candidate` is treated as the schema token for
proposed material. The predecessor enum will not be silently changed to add a
second synonymous `proposed` token.

## Deferred work

The following remain deferred exactly as required:

- Slice 38C minimal admitted built-in action-root registry;
- Slice 38D participant-role identity and registry;
- Slice 38E predicate-frame constraints and role compatibility;
- Slice 38F capability-family references and effect boundaries;
- Slice 38G predicate, role, and frame candidate proposal;
- Slice 38H disabled integration and Slice 38 closeout;
- Slice 39 CandidateMeaning construction;
- Slice 40 gate selection, clarification, refusal, and blocked progression.

## Forbidden authority

Slice 38B performs no registry population. It does not recognize general verbs, select predicates, assign roles,
complete frames, infer intent, choose a nearest known action root, route a
capability, invoke a tool, perform an action, validate evidence, access memory,
render output, authorize delivery, or claim production readiness.

## Test and acceptance ruling

Acceptance requires:

1. exact parent and path containment;
2. protected predecessor SHA-256 success;
3. Slice 38B behavior and adversarial tests;
4. Slice 38B independent verifier success;
5. inherited Slices 30 through 38A behavior regressions;
6. Slice 24 acceptance and Slice 25 hygiene checks in the proper controlled
   phase;
7. exact repository-state and source-cache checks;
8. all population, routing, action, memory, rendering, and delivery counters at
   zero;
9. a complete live-machine result packet returned by Nic for inspection.

Assistant-side isolated checks are packaging and defect-prevention evidence
only. They are not live Forge acceptance.
