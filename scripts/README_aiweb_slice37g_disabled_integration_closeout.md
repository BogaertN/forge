# Slice 37G Disabled Integration and Slice 37 Closeout

This increment is additive. It does not modify any accepted Slice 35, Slice 36
or Slice 37 predecessor file.

The package `disabled_structural_concept_bootstrap` provides an inert public
surface. Import has no execution effect. The default state refuses all
invocations. An enabled run requires an explicit exact fixture invocation and
the exact accepted Slice 37F proposal profile.

Five closed synthetic fixtures prove:

- one exact concept and sense candidate;
- two unresolved concept and sense candidates;
- a known but unmapped exact term;
- a reviewed unsupported exact term;
- no exact controlled occurrence.

Each run preserves the full eight-stage source-to-structure-to-registry chain,
returns immutable records, and records no CandidateMeaning, selection, truth,
permission, route, tool, action, memory, rendering or delivery.

The independent verifier protects every file introduced from Slice 35A through
Slice 37F, runs the Slice 37G behavior suite, executes all inherited tests, and
proves the exact pre-Slice-37 commit and tree in a disposable recovery clone.

This is not a public route, a general-language release, or production
readiness.

Permanent closeout boundaries include: surface term != concept; scale != authority; semantic relation type != relation fact; CandidateMeaning is not created.
