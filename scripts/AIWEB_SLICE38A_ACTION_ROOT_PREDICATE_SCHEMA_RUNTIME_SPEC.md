# AI.Web Slice 38A — Action-Root and Predicate-Identity Core Schema Runtime Specification

## Accepted parent

- HEAD: `f891a33487ea8bc811243627f1d834be7a43f972`
- Tree: `f087c3f6cec8caecc19539628b1d4ab08b4918c1`
- Subject: `Slice 37G disabled integration and Slice 37 closeout`

## Installed authority

Slice 38A installs immutable schema contracts for Forge-owned action-root and
predicate identities. It installs no action-root registry entries and no live
predicate identities. It provides deterministic record identity, scope,
non-scope, version, provenance, lifecycle visibility, unknown-state policy,
and explicit dependency markers for later roles, frames, effect boundaries,
and capability-family references.

## Permanent boundaries

- surface verb is not action-root identity;
- action concept is not predicate identity;
- semantic relation is not participant role;
- predicate identity is not predicate-frame identity;
- complete schema is not a complete frame;
- complete frame is not selected meaning;
- request is not authorization;
- report is not evidence;
- verification predicate is not verified status;
- memory predicate is not memory access;
- delivery predicate is not delivery authority;
- installation predicate is not code-application authority;
- capability reference is not route;
- effect classification is not execution;
- unknown predicate is not the nearest known predicate;
- scale is not authority.

Slice 38A is disabled by default, offline, deterministic, standard-library
only, read-only, non-LLM, non-routing, non-executing, and not production-ready.
It creates no CandidateMeaning, selected predicate, selected frame, participant
assignment, evidence validity, memory operation, tool invocation, action,
rendering, delivery, or release authority.
