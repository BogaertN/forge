# Forge Command Patch Proposal — generated by forge-command-approve-proposal
# Command    : update-agent-file
# Function   : cmd_update_agent_file
# Description: update agent file
# Approved   : 20260522_100047
#
# This file tells you EXACTLY what to add to main.py.
# Nothing is changed automatically — patch-apply puts you in control.
#
# ── FUNCTION TO ADD TO main.py ──────────────────────────────────────
# Add this function before the 'if __name__ == "__main__":' line:

def cmd_update_agent_file(session_id: str):
    """
    update agent file

    Generated by forge-command-propose on 20260522_100047
    Install via: forge-command-approve-proposal update_agent_file
    then patch-review, patch-preflight, patch-apply
    """
    from agents.forge.memory import write_audit_entry
    print()
    _title = 'update agent file'
    print(f'── {_title} ──')
    print("  Status: EXECUTED")
    print("  TODO: Replace this with real implementation")
    print("─" * 60)
    print()
    write_audit_entry(
        session_id=session_id,
        tool="CMD_UPDATE_AGENT_FILE_EXECUTED",
        path="-", lines="-",
        reason="llm-generated command executed"
    )

# ── ROUTING TO ADD IN COMMAND LOOP ──────────────────────────────────
# Add this in the user_input routing section:

#             if user_input.lower() == "update-agent-file":
#                 cmd_update_agent_file(session_id)
#                 continue

# ── EXPECTED COMMANDS LIST ───────────────────────────────────────────
# Add to FORGE_EXPECTED_COMMANDS:
#     "update-agent-file",

# ── TOOL REGISTRY ────────────────────────────────────────────────────
# Add to config/tool_registry.json under 'tools':
#     "cmd_update_agent_file": {"description": "update agent file", "executes": false, "trust_level": 5.0, "writes": false, "generated_by": "forge-command-propose", "generated_at": "20260522_100047"}
